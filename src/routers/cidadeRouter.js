/* const { Router } = require("express");

const router = Router();

//busca todos os admins
router.get("/");

// funcao buscar unico
router.get("/:id");

//funcao de criar
router.post("/");

// funcao de editar
router.put("/:id");

// funcao de deletar
router.delete("/:id");

module.exports = router;
 */